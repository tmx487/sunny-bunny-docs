В C# ключевые слова `static`, `const` и `readonly` часто путают, потому что все они **ограничивают изменение переменных**, но делают это по-разному. Вот чёткое сравнение:

---

## 🔹 1. `static`

- Обозначает, что **поле принадлежит типу**, а не экземпляру.
    
- Инициализируется один раз и **общее для всех объектов**.
    
- Может быть изменяемым (если не `const` или `readonly`).
    

```csharp
class Example
{
    public static int Count = 0;

    public Example()
    {
        Count++; // общее для всех экземпляров
    }
}
```

---

## 🔹 2. `const`

- Обозначает **компиляторную константу**.
    
- Значение **должно быть известно на этапе компиляции**.
    
- Автоматически `static`.
    
- **Нельзя изменить**, и **нельзя инициализировать в конструкторе**.
    

```csharp
class Example
{
    public const double Pi = 3.14159;
}
```

> ⚠ `const` — не подходит для значений, которые могут измениться (например, настройки, даты и т.п.).

---

## 🔹 3. `readonly`

- Значение можно присвоить:
    
    - **в момент объявления**
        
    - или **в конструкторе** (что позволяет разным объектам иметь разное значение).
        
- После инициализации — **нельзя изменить**.
    
- Может быть экземплярным или `static readonly`.
    

```csharp
class Example
{
    public readonly int CreatedAt;

    public Example()
    {
        CreatedAt = Environment.TickCount;
    }
}
```

---

### 🧠 Сравнительная таблица

|Ключевое слово|Можно изменить?|Где инициализируется|Общее для всех объектов?|
|---|---|---|---|
|`static`|✅ (если не const/readonly)|В любом месте класса|✅|
|`const`|❌|Только при объявлении|✅|
|`readonly`|❌ (после конструктора)|При объявлении или в конструкторе|❌ (если не `static`)|

---

### 💡 Когда использовать:

- `const` — для **вечных значений**, типа `Pi`, `DaysInWeek`.
    
- `readonly` — для значений, которые задаются **один раз при создании** объекта (например, `CreatedAt`, `Id`).
    
- `static` — когда нужно **общее поле или метод** для всех экземпляров (например, `InstanceCount` или `Math.Sqrt()`).
    

Хочешь практическую задачку, чтобы закрепить разницу?